# Public base path for API requests (must match backend API_PREFIX/API_VERSION)
NEXT_PUBLIC_API_URL=
# Backend origin used by Next.js rewrites (server-side only)
API_URL=
# Backend origin for Socket.IO messaging (e.g. http://localhost:8000)
NEXT_PUBLIC_WS_URL=
